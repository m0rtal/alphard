# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
*.egg-info/
.installed.cfg
*.egg
venv/
.venv/
env/
.env
.env.local
.env.production
.env.staging

# Poetry
.python-version
poetry.lock

# Tests
.pytest_cache/
.coverage
htmlcov/
.tox/

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# Docker
.docker/
docker-compose.override.yml

# Secrets (CRITICAL: never commit)
*.pem
*.key
*.crt
*.p12
*.pfx
secrets/
*.secret

# Local data
/data/
data_local/
backtest_results/
logs/
*.log

# OS
.DS_Store
Thumbs.db

# Build artifacts
dist/
build/
*.egg-info/

# Agent scratchpads (auto-generated)
.alphard/
*.swp

# Stray clones left behind inside the worktree (e.g. `git clone . alphard/`).
# A nested repo of the same tree is never intentional and confuses `git status`.
# If you really need one, use `git worktree` (see .worktrees/ above).
/alphard/

# Database backups (large TSV files)
backups/
*.tsv
*.tar.gz

# Kanban worker worktrees (ephemeral, never commit)
.worktrees/
