# Pinned deps for Phase 0 — no poetry install needed
# In Phase 1+ we'll use poetry properly when deps are stable
pydantic>=2.5,<3.0
pytest>=7.4,<8.0
pytest-cov>=4.1,<5.0
# CI 2026-08-14 found black 24.10.0 (local) vs 24.11+ (CI runner) diverge in formatting.
# Pin exact patch version so local == CI == CI runner for byte-identical diffs.
black==24.10.0
flake8>=7.0,<8.0
mypy>=1.10,<2.0

# Phase 1.1 (data agent) — stdlib-first, single external HTTP client.
requests>=2.31,<3.0
python-dateutil>=2.8,<3.0
# pydantic-settings is optional: TinkoffDataLoader reads env vars directly via
# os.environ. We pin the upper bound so a future v3 release (breaking change)
# cannot accidentally land in production.
pydantic-settings>=2.0,<3.0

# Phase 1.1 dev deps — property-based contract tests.
hypothesis>=6.100,<7.0

# Phase 1.1 production deps — only when Postgres is in use. NOT imported
# by default; the module imports ``psycopg`` lazily so CI without Postgres
# doesn't choke. Pin the major version: psycopg 3 has a different API
# surface than psycopg2, so mixing them silently corrupts connections.
# Uncomment locally:
psycopg[binary]>=3.1,<4.0  # Phase 1.2 — Postgres integration test
# mypy stubs (Phase 1.2)
types-requests>=2.31,<3.0

# Tinkoff Invest SDK (Phase 1.1) — Apache-2.0
# Install: pip install t-tech-investments --index-url https://opensource.tbank.ru/api/v4/projects/238/packages/pypi/simple
t-tech-investments>=1.49,<2.0

PyYAML>=6.0
